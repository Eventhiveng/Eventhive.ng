[build]
  # Default settings for static branches (HTML, CSS, JS)
  publish = "/"
  command = ""

# Specific context for the master branch (React/Vite)
[context.production]  # This is equivalent to [context."master"]
  command = "npm run build"
  publish = ".next"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200